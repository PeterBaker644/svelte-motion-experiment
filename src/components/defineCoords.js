
function defineCoords(card, {hand, graveyard, table}){
  // read in the lists in from the board state.
  // for each list
  if (card === table[0]) {

  } else if (graveyard.includes(card)) {

  }
  console.log(hand, graveyard);
}

export default defineCoords;