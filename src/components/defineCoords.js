import board from './stores'

$: ({disabled, hand, graveyard, table} = $board);

function defineCoords(id){
  // read in the lists in from the board state.
  // for each list
}

export default defineCoords;